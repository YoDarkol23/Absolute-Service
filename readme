


!!!ЭТА ТОЛЬКО ПРИКИДКА, Я ЕЩЁ НИЧЕГО НЕ СДЕЛАЛ!!!!!




Структура проекта testCarDelivery
Это клиент-серверное консольное приложение для сервиса доставки автомобилей.
Сервер обрабатывает запросы (список авто, расчёт таможни), клиент — показывает дружелюбное меню.

Общая структура
testCarDelivery/
├── CMakeLists.txt        ← Главный файл сборки
├── common/               ← Общие компоненты (для клиента и сервера)
├── server/               ← Серверная часть
├── client/               ← Клиентская часть
└── data/                 ← Данные (только для сервера)

CMakeLists.txt
Назначение:
Управляет сборкой всего проекта. Указывает, какие файлы компилировать, какие библиотеки подключать (Boost), и куда копировать данные.
Что можно менять:
Добавлять новые .cpp файлы в секции add_executable(server ...) или add_executable(client ...).
Менять количество потоков (числа 6 и 2) — но это лучше делать в коде (server/server.cpp).
Если добавишь новые данные (например, tariffs.json), обнови строку:
file(COPY data/ DESTINATION ${CMAKE_BINARY_DIR}/data)
Важно:
Не удаляй pthread в target_link_libraries — он нужен для многопоточности в Linux.

Папка common/
Назначение:
Содержит код, который используется и сервером, и клиентом. Например: отправка HTTP-запросов, чтение файлов.
Файлы:
common/utils.hpp
Что делает: Объявляет общие функции.
Можно добавлять: Новые функции, например parse_json(), validate_input().
common/utils.cpp
Что делает: Реализует функции из utils.hpp.
Можно улучшать: Добавить обработку ошибок, таймауты, логирование.
💡 Совет: Если функция нужна только серверу или только клиенту — не клади её сюда!

Папка server/
Назначение:
Содержит сервер — программу, которая:
Слушает порт 8080 (клиенты)
Обрабатывает запросы в многопоточном режиме
Читает данные из data/
Возвращает JSON-ответы
Файлы:
server/main.cpp
Что делает: Точка входа сервера. Создаёт и запускает объект CarDeliveryServer.
server/server.hpp
Что делает: Объявляет классы ThreadPool и CarDeliveryServer.
Можно менять: Количество потоков в пуле (изменяй значения в main.cpp или сделай параметрами конструктора).
server/server.cpp
Что делает: Реализует логику сервера:
Приём подключений
Распределение по пулам потоков
Парсинг HTTP-запросов
Можно расширять:
Добавь новые пути: GET /search, POST /calculate
Улучши парсинг HTTP (сейчас упрощённый)
server/handlers.hpp и server/handlers.cpp
Что делают: Содержат бизнес-логику:
handle_get_cars() — возвращает список авто
handle_admin_request() — обрабатывает админ-запросы
Сюда добавляй:
Расчёт таможенных платежей
Фильтрацию по параметрам
Редактирование cars.json (для админа)
💡 Это главное место для доработки! Вся логика расчётов будет здесь.

Папка client/
Назначение:
Содержит клиент — консольную программу с меню, которая:
Отправляет запросы на сервер
Показывает результат пользователю
Файлы:
client/main.cpp
Что делает: Показывает текстовое меню, принимает выбор пользователя, вызывает функции из client.hpp.
Можно расширять:
Добавляй новые пункты меню: «2. Поиск по фильтрам», «3. Расчёт доставки»
Делай цикл вместо однократного запуска
client/client.hpp
Что делает: Объявляет функции для работы с сервером.
Можно добавлять:
search_cars(const Filter& f),
calculate_customs(const Car& c),
admin_login(string, string)
client/client.cpp
Что делает: Реализует HTTP-запросы к серверу.
Можно улучшать:
Парсинг JSON-ответов (сейчас выводится «как есть»)
Красивый вывод в консоль (таблицы, цвета)

Папка data/
Назначение:
Хранит данные, которые использует сервер. Клиент к этой папке не имеет доступа.
Файлы:
data/cars.json
Что делает: База данных автомобилей в формате JSON.
Можно менять:
Добавлять/удалять авто
Менять структуру (добавлять поля: price, country, engine_type)
Важно:
Сервер ищет этот файл относительно своей рабочей директории.
При запуске из build/ путь — ./data/cars.json.
💡 В будущем можно добавить: 

tariffs.json — правила расчёта таможни
admin_users.json — учётные записи админов

Как собрать и запустить программу
Требования
Linux (Ubuntu/Debian рекомендуется)
Установленные пакеты
sudo apt install build-essential cmake libboost-all-dev
Перейди в папку проекта
cd testCarDelivery
Собери проект
mkdir build
cd build
cmake ..
make
После этого в папке build/ появятся:
server — исполняемый файл сервера
client — исполняемый файл клиента
data/ — скопированная папка с данными

Запусти сервер (в первом терминале)
./server
Ты увидишь:
🚀 Сервер запущен!
   Обычные запросы: http://localhost:8080
   Админка:         http://localhost:8081
Ожидание подключений...

Запусти клиент (во втором терминале)
./client
Выбери пункт 1 → получишь JSON со списком авто.

Остановка
Сервер: нажми Ctrl+C в его терминале
Клиент: завершается автоматически после выполнения

пошаговая инструкция для тебя и друга (сори но всё под линукс)
Соберите проект у себя (см. выше). Отправьте только файл  ./client
Ты (сервер):
Узнай свой IP: hostname -I → например, 192.168.1.35
Запусти сервер: cd build  → ./server
Дождись подключения — увидишь лог в терминале

Клиент после получения должен выполнить 
chmod +x client
./client <ваш_IP>
Если подключение не работает, убедитесь, что на сервере разрешён порт 8080.
sudo ufw allow 8080/tcp
Или временно отключите фаервол для теста
sudo ufw disable

Тестирование админки (опционально)
Админка слушает порт 8081. Проверить можно через curl:
curl -X POST http://localhost:8081 -d '{"action":"test"}'
Ответ:
{"status": "success", "message": "Admin command executed"}
💡 Позже сюда можно добавить проверку пароля и редактирование scars.json.
